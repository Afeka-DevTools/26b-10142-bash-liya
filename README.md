# חברי הצוות  

ליה ג'וני <214695173>  

ירין לודר <212948376>

# חלוקת עבודה  

### הסקריפטים של ליה:
1.**גיבוי תכולת תיקיה ל-`tar.gz`** (`backup.sh`)  
2. **יצירת סיסמה רנדומלית** (`generate_password.sh`) 
3. **הצגת מידע על המשתמש** (`user_info.sh`) 
4. **סיכום קבצים ותיקיות** (`summary.sh`) 
5. **מיון שורות בקובץ** (`sort_file.sh`) 

### הסקריפטים של ירין:
1. **בדיקת חיבוריות לאינטרנט** (`check_internet.sh`) 
2. **הוספת תחילית לקבצי טקסט** (`add_prefix.sh`) 
3. **מחיקת קבצים ישנים** (`delete_old_files.sh`) 
4. **שליפת מזג אוויר מעודכן** (`weather.sh`) 
5. **שעון זמן אמת ב-ASCII** (`ascii_clock.sh`) 

---
